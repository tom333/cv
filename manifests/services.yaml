apiVersion: v1
kind: Service
metadata:
  name: cv
  namespace: cv
  labels:
    name: cv
spec:
  type: ClusterIP
  selector:
    name: cv
  ports:
  - port: 80
    name: http
    targetPort: 80
    protocol: TCP
